#include <bits/stdc++.h>
using namespace std;
// 拼尽全力无法战胜，所以：不可以，总司令！
int main() {
	freopen("cons.in", "r", stdin);
	freopen("cons.out", "w", stdout);
	cin.tie(nullptr)->sync_with_stdio(false);
	int _T;
	cin >> _T;
	for(int n, C; _T--; cout.put('\n')) {
		cin >> n >> C;
		cout << "-1";
	}
	cout.flush(); return 0;
}
