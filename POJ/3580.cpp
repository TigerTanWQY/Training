#include <bits/stdc++.h>
using namespace std;

constexpr const int N = 1e5 + 3;
int a[N];

int main() {
	cin.tie(nullptr)->sync_with_stdio(false);
	int n;
	cin >> n;
	for(int i = 1; i <= n; ++i)
		cin >> a[i];
	;
	cout << endl;
	return 0;
}
