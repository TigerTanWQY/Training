#include <iostream>
#include <string>
using namespace std;

const int P = 998244353;
int c[13];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	string n;
	int m;
	cin >> n >> m;
	for(int i = 1; i <= m; ++i)
		cin >> c[i];
	int len = n.size();
	;
	return 0;
}