#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(nullptr)->sync_with_stdio(false);
	int n, m, _q;
	cin >> n >> m >> _q;
	for(int a, b, l, r; _q--; cout.put('\n')) {
		cin >> a >> b >> l >> r;
	}
	cout.flush(); return 0;
}
