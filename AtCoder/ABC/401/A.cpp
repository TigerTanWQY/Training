#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(nullptr)->sync_with_stdio(false);
	int n;
	cin >> n;
	if(200 <= n && n < 300)
		cout << "Success";
	else
		cout << "Failure";
	cout << endl; return 0;
}
