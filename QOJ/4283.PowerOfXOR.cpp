#include <bits/stdc++.h>
using namespace std;
using ULL = unsigned long long;

constexpr const int P = 1e9 + 7, N = 47;
ULL a[N];

int main() {
	cin.tie(nullptr)->sync_with_stdio(false);
	int n, k;
	cin >> n >> k;
	for(int i = 1; i <= n; ++i)
		cin >> a[i];
	cout << endl;
	return 0;
}
