#include <cstdio>
using namespace std;

int main()
{
	int T;
	scanf("%d", &T);
	while(T--)
	{
		int n, q;
		scanf("%d%d", &n, &q);
		while(q--)
		{
			int x, y;
			scanf("%d%d", &x, &y);
			;
		}
	}
	return 0;
}