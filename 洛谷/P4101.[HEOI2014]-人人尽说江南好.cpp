#include <iostream>
using namespace std;

constexpr const int N = 1e9 + 3;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int _T;
	cin >> _T;
	for(int n, m; _T--; cout.put('\n')) {
		cin >> n >> m;
		;
	}
	cout.flush();
	return 0;
}
