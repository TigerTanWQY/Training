#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(nullptr)->sync_with_stdio(false);
	int n, d, m; string s;
	cin >> n >> d >> s >> m;
	for(int i, j; m--; ) {
		char hi, hj;
		cin >> i >> hi >> j >> hj;
	}
	cout << "ABA" << endl;
	return 0;
}
